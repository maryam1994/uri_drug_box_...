<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
    <div class="field">
      <% if @prescription.blank? %>
      <%= f.label :prescription_id %><br>
      <%= f.number_field :prescription_id %>
      <%else%>
      <%= f.hidden_field :prescription_id , :value =>  @prescription.id %>
      <%end%>
      
    </div>
    
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
